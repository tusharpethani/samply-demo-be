# samply-demo-backend

## Available Scripts

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:8080](http://localhost:8080) to view it in your browser.

Also find attached Postman API collection (todo-demo.postman_collection.json) to get all the API's end-point.

You need to setup the MySQL database in your local system to access the database tables.

### Sample user credential

email: user1@yopmail.com\
password: Test@1234
